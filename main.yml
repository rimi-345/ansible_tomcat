--- 
- name: Deploy
  hosts: all
  become: true
  vars_files:
    - tomcat_credentials.yml 
  tasks: 
  - name: Tomcat
    command: curl -v -u "{{ user_name }}":"{{ password }}" -T /home/ubuntu/actions-runner/_work/ansible_tomcat/ansible_tomcat/target/WebApp-pipeline.war "http://65.1.91.93:8080//manager/text/deploy?path=/web_newr&update=true"
